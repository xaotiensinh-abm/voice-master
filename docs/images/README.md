# Ảnh giao diện

Đặt ảnh chụp màn hình ứng dụng vào đây với đúng tên file để README tự hiển thị:

| Tên file | Nội dung nên chụp |
|----------|-------------------|
| `01-create-voice.png` | Màn "Tạo giọng nói" (dropdown chọn giọng, ô nhập text, nút chèn cảm xúc, nút Tạo MP3) |
| `02-agent-connect.png` | Trang "🔌 Kết nối Agent / API" (endpoint, tool, ví dụ vòng lặp) |
| `03-model-download.png` | Màn/cổng tải mô hình VieNeu lần đầu |
| `04-settings.png` | Cài đặt → mục Mô hình |

Gợi ý: mở `http://localhost:5173`, chụp ở bề ngang ~1280px cho đẹp. Định dạng PNG.
