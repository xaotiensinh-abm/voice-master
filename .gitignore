# Dependencies
node_modules/
.pnpm-store/

# Build outputs
dist/
dist-electron/
release/

# Python
backend/.venv/
backend/__pycache__/
backend/**/__pycache__/
backend/*.pyc
*.egg-info/

# IDE
.vscode/
.idea/

# OS
.DS_Store
Thumbs.db
desktop.ini

# Environment
.env
.env.local
.env.*.local

# App data (local)
*.db
runtime.json

# Logs
*.log

# Temp
temp/
*.tmp

# Extracted spec (not needed in production)
extracted/

# Model caches (too large for git) — anchored to repo root so the
# backend/models/ Python package (schemas etc.) is NOT excluded.
/models/
/voicepacks/

# Archive
*.zip

# Author signing keys (NEVER commit)
tools/keys/

# Release build staging (portable python/backend/ffmpeg runtime)
release-staging/
