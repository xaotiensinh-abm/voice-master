# CPU-only dependency set for the packaged .exe (NO torch — VieNeu v3 Turbo runs
# via ONNX Runtime on CPU). Used by scripts/build_backend_runtime.ps1.
# If the build's import self-check reports a missing module, add it here.
fastapi>=0.111.0
uvicorn[standard]>=0.30.0
pydantic>=2.7.0
aiosqlite>=0.20.0
httpx>=0.27.0
keyring>=25.2.0
python-multipart>=0.0.9
mcp>=1.28.0
cryptography>=42.0.0
huggingface_hub>=0.23.0
onnxruntime>=1.17.0
vieneu
